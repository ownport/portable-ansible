- install: ansible==2.5.2.0
  target: ansible/
  no-deps: true

- install: PyYAML==3.12
  target: ansible/
  no-deps: true

- install: paramiko==2.4.1
  target: ansible/
  no-deps: true

- install: jinja2==2.10
  target: ansible/

- install: six==1.11.0
  target: ansible/
  no-deps: true
