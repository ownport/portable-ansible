- install: ansible==2.4.2.0
  target: ansible/
  no-deps: true

- install: PyYAML==3.12
  target: ansible/
  no-deps: true

- install: paramiko==2.1.1
  target: ansible/
  no-deps: true

- install: jinja2==2.9.4
  target: ansible/

- install: six==1.10.0
  target: ansible/
  no-deps: true
